version: '3.8'

services:
  api:
    build: .
    container_name: assistia-api
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - OMP_NUM_THREADS=2
    volumes:
      - ./models:/app/models
    restart: unless-stopped